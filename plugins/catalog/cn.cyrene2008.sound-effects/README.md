# 来点音效

由 Cyrene2008 提供的官方示例插件，插件 API 版本 1.0。

安装后，在 Dock 的“插件”页面打开“音效”扩展页，可以：

- 开关音效功能；
- 调整音量；
- 分别为随机点名、翻牌点名、抽奖选择本地音频；
- 选择“每次操作播放一次”或“每个结果播放一次”。

支持 MP3、M4A、WAV、FLAC、OGG，单个音频文件不超过 16 MB。音频数据仅存储在本机插件命名空间中。

该插件也作为 SDK 的 `sound-effects` 模板，用于演示 Worker 事件、插件存储、音频选择、音频播放和独立插件页面。
